import 'package:flutter_test/flutter_test.dart';

void main() {
  test('bible_reader_core package is properly configured', () {
    // This test ensures the package is set up correctly
    expect(true, isTrue);
  });
}
