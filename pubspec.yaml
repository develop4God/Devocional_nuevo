name: devocional_nuevo
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  shared_preferences: ^2.2.3
  screenshot: ^3.0.0
  share_plus: ^11.0.0
  auto_size_text: ^3.0.0
  path_provider: ^2.1.3
  provider: ^6.0.0 # O la versión más reciente que se muestre en pub.dev
  intl: ^0.19.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ 