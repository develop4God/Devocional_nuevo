# Test configuration - automatically used by flutter test
tags:
  critical:
    timeout: 30s
  unit:
    timeout: 10s
  slow:
    timeout: 5m
  bloc:
    # Used alongside critical/unit/slow tags for organization
    # Example: @Tags(['critical', 'bloc'])

# Default timeout for untagged tests
timeout: 1m
